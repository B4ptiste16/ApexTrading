# APEX Server dependencies
# Install with: pip install -r server/requirements.txt
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
bcrypt>=4.1.2
PyJWT>=2.8.0
pydantic>=2.6.0
python-multipart>=0.0.9
# V7.1+ — Fernet symmetric encryption for at-rest broker credentials
cryptography>=42.0.0
# V4.2.0 — Stripe payment gateway for credit top-ups
stripe>=8.0.0
