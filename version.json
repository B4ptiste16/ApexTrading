{
  "version": "4.6.65",
  "notes": "v4.6.65 — App opens fully loaded (no more load-on-click)\n\n⚡ EAGER LOADING\n• The app now loads the Overview AND every bot tab right at launch and keeps them all refreshed in the background — so when you click a tab it's already populated instead of starting to load. Combined with the v4.6.64 broker preload, switching Alpaca<->IBKR is much faster too.\n\n(The currently-selected broker's tabs are always live; the other broker is kept warm in the background. A dedicated full-screen loading splash is coming next.)\n\nUpdate via the in-app banner.\n"
}
