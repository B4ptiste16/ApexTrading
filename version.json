{
  "version": "4.2.1",
  "notes": "v4.2.1 — hotfix: slot order + version loop\n\n🔧 FIXES\n• API slot assignment now persists across restarts — custom bot keys no longer reset to DAY/SHORT positions after relaunch\n• Version loop after updates fixed — updater correctly reads the installed version so it no longer re-prompts to install the same update"
}
