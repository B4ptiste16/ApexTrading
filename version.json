{
  "version": "1.1.8",
  "notes": "V7.1.8: explicit QUIT button in the header + fixed auto-update relauncher (taskkill without /T) + Overview crash fix (renamed _period_combo attribute that was shadowing the chart's combo method). Install this manually once and from then on use the in-app UPDATE AVAILABLE banner for everything else."
}
